services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      - backend
    ports:
      - "${APP_PORT:-80}:80"
    restart: unless-stopped
  backend:
    build: ./backend
    restart: unless-stopped
